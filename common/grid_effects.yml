
      - addressable_lambda: 
          name: "Gradient Rows"
          update_interval: 16ms
          lambda: |-
            #include "glow/Gradients.h"
            static glow::Gradients gradients;

            if (initial_run) {
              gradients.setup(&it, current_color);
            }

            gradients.check_interval(millis());
            gradients.as_rows();

      - addressable_lambda: 
          name: "Gradient Rows Flat"
          update_interval: 16ms
          lambda: |-
            
            #include "glow/Gradients.h"
            static glow::Gradients gradients;

            if (initial_run) {
              gradients.setup(&it, current_color);
            }

            gradients.check_interval(millis());
            gradients.as_rows_flat();

      - addressable_lambda: 
          name: "Gradient Columns"
          update_interval: 16ms
          lambda: |-
            
            #include "glow/Gradients.h"
            static glow::Gradients gradients;

            if (initial_run) {
              gradients.setup(&it, current_color);
            }

            gradients.check_interval(millis());
            gradients.as_columns();

      - addressable_lambda: 
          name: "Gradient Columns Flat"
          update_interval: 16ms
          lambda: |-
            
            #include "glow/Gradients.h"
            static glow::Gradients gradients;

            if (initial_run) {
              gradients.setup(&it, current_color);
            }

            gradients.check_interval(millis());
            gradients.as_columns_flat();

      - addressable_lambda: 
          name: "Gradient Diagonal"
          update_interval: 16ms
          lambda: |-
            
            #include "glow/Gradients.h"
            static glow::Gradients gradients;

            if (initial_run) {
              gradients.setup(&it, current_color);
            }

            gradients.check_interval(millis());
            gradients.as_diagonal();
