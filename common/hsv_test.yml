update_interval: 5000ms
lambda: |-
  // static uint8_t gradient_state = 0;
  static ESPHSVColor hsv (0, 255, 255);
  if (initial_run) {
    it.set_correction(1.0, 1.0, 1.0);
    hsv.hue = 0;
  }

  hsv.saturation = 255;
  hsv.value = 150;
  // if (gradient_state==0){
  //   hsv.hue = 0;
  // } else if (gradient_state==1){
  //   hsv.hue = 85;
  // } else if (gradient_state==2){
  //   hsv.hue = 170;
  // }

  int size = it.size();
  uint8_t delta = 128 / size;

  for (int i = 0; i < size; i++) {
    it[i] = hsv;
    hsv.value -= delta;
  }       

  hsv.hue += 8;
  // gradient_state++;
  // if (gradient_state > 2) {
  //   gradient_state = 0;
  // }
